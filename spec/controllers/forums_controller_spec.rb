require 'spec_helper'

describe FormsController do
  describe "GET show" do
    it "shows a post"
  end

  describe "POST create" do
    context "with a recently registered user"
      it "has a pre-populated question, what does your organization need help in"
      it "redirects to the user's profile"
  end
end